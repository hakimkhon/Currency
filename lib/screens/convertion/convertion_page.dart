import 'package:flutter/material.dart';

class ConvertionPage extends StatelessWidget {
  const ConvertionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        bottom: false,
        child: Center(
          child: Text("Convertion page"),
        ),
      ),
    );
  }
}
